# index.html

Entry-point HTML document that mounts the application shell. It links the consolidated stylesheet, exposes placeholders for the dynamically rendered content area and bottom control panel, and loads the module bootstrapper that wires the dependency container.[^1]

[^1]: Global containers and module script [index.html#L1-L12](../index.html#L1-L12)
