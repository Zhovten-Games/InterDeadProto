# Logging

Core logging utilities provide infrastructure-agnostic defaults. See [NullLogger](NullLogger.md) for the default no-op implementation used throughout core services when no adapter is supplied.
