"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HugoConfigSourceAdapter = void 0;
const DEFAULT_SELECTORS = [
    "[data-modal-trigger]",
    "[data-cta-anchor]",
    "[data-auth-button]",
    ".gm-slider__arrow",
];
const DEFAULT_TITLE_SELECTORS = ["[data-decorative-title]"];
class HugoConfigSourceAdapter {
    constructor(documentRef = document) {
        this.documentRef = documentRef;
    }
    load() {
        const bodyMarker = this.documentRef.body?.hasAttribute("data-interdead-framework")
            ? this.documentRef.body
            : null;
        const marker = bodyMarker ||
            this.documentRef.querySelector("[data-interdead-framework]");
        if (!marker) {
            return {};
        }
        const membraneEnabled = marker.dataset.frameworkMembrane !== "false";
        const selectors = marker.dataset.frameworkMembraneSelectors
            ? marker.dataset.frameworkMembraneSelectors
                .split(",")
                .map((item) => item.trim())
                .filter(Boolean)
            : DEFAULT_SELECTORS;
        const membraneConfig = {
            canvasClassName: marker.dataset.frameworkMembraneCanvasClass || "gm-membrane-canvas",
            activeBodyClass: marker.dataset.frameworkMembraneActiveClass || "gm-membrane-active",
            interactionSelectors: selectors,
            reducedMotionMode: marker.dataset.frameworkReducedMotionMode === "disable"
                ? "disable"
                : marker.dataset.frameworkReducedMotionMode === "full"
                    ? "full"
                    : "minimal",
            pulseEventName: marker.dataset.frameworkMembranePulseEvent ||
                "interdead:membrane-pulse",
        };
        const titleEnabled = marker.dataset.frameworkDecorativeTitle === "true";
        const titleSelectors = marker.dataset.frameworkDecorativeTitleSelectors
            ? marker.dataset.frameworkDecorativeTitleSelectors
                .split(",")
                .map((item) => item.trim())
                .filter(Boolean)
            : DEFAULT_TITLE_SELECTORS;
        const decorativeTitleConfig = {
            selectors: titleSelectors,
            membranePulseEventName: marker.dataset.frameworkDecorativeTitlePulseEvent ||
                "interdead:membrane-pulse",
            pulseHighlightDurationMs: marker.dataset.frameworkDecorativeTitlePulseMs
                ? Number(marker.dataset.frameworkDecorativeTitlePulseMs)
                : 220,
            localeFontFamilies: {
                default: marker.dataset.frameworkDecorativeTitleFontDefault ||
                    "var(--font-heading)",
                en: marker.dataset.frameworkDecorativeTitleFontEn ||
                    '"Pirata One", system-ui',
                ru: marker.dataset.frameworkDecorativeTitleFontRu ||
                    "var(--font-heading)",
                uk: marker.dataset.frameworkDecorativeTitleFontUk ||
                    "var(--font-heading)",
                ja: marker.dataset.frameworkDecorativeTitleFontJa ||
                    "var(--font-heading)",
            },
            patternPrimaryText: marker.dataset.frameworkDecorativeTitlePatternPrimary,
            patternSecondaryText: marker.dataset.frameworkDecorativeTitlePatternSecondary,
        };
        return {
            enabledFeatures: {
                membrane: membraneEnabled,
                decorativeTitle: titleEnabled,
            },
            featureOptions: {
                membrane: membraneConfig,
                decorativeTitle: decorativeTitleConfig,
            },
        };
    }
}
exports.HugoConfigSourceAdapter = HugoConfigSourceAdapter;
//# sourceMappingURL=hugo-config-source-adapter.js.map