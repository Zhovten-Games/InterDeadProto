import type { MembraneConfig } from "../../contracts/framework-config.js";
export declare class MembraneRenderer {
    private readonly canvas;
    private readonly windowRef;
    private readonly ctx;
    private width;
    private height;
    private pulse;
    private pulseAge;
    private pulseX;
    private pulseY;
    constructor(canvas: HTMLCanvasElement, windowRef: Window);
    resize(): void;
    triggerPulse(xRatio: number, yRatio: number): void;
    draw(time: number, config?: MembraneConfig): void;
    private drawScreenVeil;
    private drawVerticalResiduals;
}
