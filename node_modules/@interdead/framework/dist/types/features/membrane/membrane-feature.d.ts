import type { FrameworkFeature } from "../../contracts/framework-feature.js";
import type { FrameworkConfig } from "../../contracts/framework-config.js";
export declare class MembraneFeature implements FrameworkFeature {
    private readonly windowRef;
    private readonly documentRef;
    readonly key: "membrane";
    private animationFrame;
    private canvas;
    private renderer;
    private boundInteractions;
    private config;
    private appliedBodyClass;
    private readonly pulseEventNameFallback;
    constructor(windowRef?: Window, documentRef?: Document);
    mount(): void;
    updateConfig(config: FrameworkConfig): void;
    destroy(): void;
    private readonly handleResize;
    private readonly tick;
    private bindInteractions;
    private unbindInteractions;
    private dispatchPulseEvent;
}
