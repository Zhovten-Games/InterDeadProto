import type { FrameworkFeature } from "../../contracts/framework-feature.js";
import type { FrameworkConfig } from "../../contracts/framework-config.js";
export declare class DecorativeTitleFeature implements FrameworkFeature {
    private readonly windowRef;
    private readonly documentRef;
    readonly key: "decorativeTitle";
    private config;
    private mountedTitles;
    private mountedHeadingSet;
    private pulseResetTimer;
    private pulseHandler;
    private documentObserver;
    private pendingBodyMountRetry;
    private pendingBodyRetryCount;
    private bodyReadyListener;
    constructor(windowRef?: Window, documentRef?: Document);
    mount(): void;
    updateConfig(config: FrameworkConfig): void;
    destroy(): void;
    private scanAndMountTitles;
    private mountSingleTitle;
    private markPulseState;
    private extractPulseDetail;
    private scheduleBodyRetry;
    private attachBodyReadyListener;
    private clearBodyReadyListener;
    private ensureStyles;
}
