import type { FrameworkConfig } from "../contracts/framework-config.js";
import type { ConfigSourcePort } from "../ports/config-source-port.js";
export interface RuntimeOptions {
    windowRef?: Window;
    documentRef?: Document;
}
export declare class FrameworkRuntime {
    private readonly configSource;
    private readonly options;
    private readonly registry;
    private readonly features;
    private config;
    private booted;
    constructor(configSource: ConfigSourcePort, options?: RuntimeOptions);
    boot(): void;
    destroy(): void;
    private applyMinimalMembraneProfile;
}
export declare function createInterdeadFramework(config?: FrameworkConfig, options?: RuntimeOptions): FrameworkRuntime;
