# proto-dev/index.html

Entry-point HTML document for the `proto-dev` implementation. It mounts the app shell, declares global containers, and loads the bootstrap module that wires dependency injection and startup flow.[^1]

[^1]: Markup structure and module script [proto-dev/index.html#L1-L12](../../proto-dev/index.html#L1-L12)
